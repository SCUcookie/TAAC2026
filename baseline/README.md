# Baseline Split

This folder is now organized into two lanes:

- `training/`: training-only code
- `evaluation/`: evaluation-only code

The original root-level files are kept for compatibility. Use the split folders when you want to review or compare the two workflows separately.
